# andydrice.com

Personal essay site. Eleventy 3.1.x, deployed to Cloudflare Workers static assets.

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `addr/andydrice.com`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
